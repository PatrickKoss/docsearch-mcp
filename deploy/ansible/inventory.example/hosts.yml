---
all:
  hosts:
    db-server:
      ansible_host: 192.168.1.10       # replace with your VM's IP or hostname
      ansible_user: ubuntu              # SSH user with sudo privileges
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
